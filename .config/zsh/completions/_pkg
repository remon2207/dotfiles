#compdef pkg

_pkg() {
  _values '' \
    'check[アップグレードできるパッケージを表示]' \
    'up[アップグレード]' 'clean[アンインストール]' \
    'unrequired[不要なパッケージを表示]' \
    'unrequired-clean[不要なパッケージをアンインストール]' \
    'list-export[パッケージリストをdotfilesに保存]' \
    'help[helpを表示]'

  return
}
