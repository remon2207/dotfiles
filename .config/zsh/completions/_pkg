#compdef pkg

_pkg() {
  _values '' \
    'check[アップグレードできるパッケージを表示]' \
    'up[アップグレード]' \
    'unrequired[不要なパッケージを表示]' \
    'unrequired-clean[不要なパッケージをアンインストール]' \
    'help[helpを表示]'

  return
}
