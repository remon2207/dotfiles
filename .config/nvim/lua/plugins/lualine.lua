require('lualine').setup {
    -- options = {
    --     -- icons_enabled = true,
    --     -- section_separators = { left = nil, right = nil}
    --     section_separators = { left = '', right = ' '},
    -- }
}
