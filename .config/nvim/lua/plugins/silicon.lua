-- local status, silicon = pcall(require, 'silicon')
-- if not status then
--   return
-- end

-- silicon.setup({
--   font = 'Hack=16',
--   theme = 'solarized'
-- })

require('silicon').setup({
  font = 'Hack=16',
  theme = 'solarized'
})
